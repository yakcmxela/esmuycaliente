import { Interactor } from "./interactor"
import { app } from "firebase"

describe("<Firebase /> Interactor", () => {
  // const { caliente } = new Interactor()
  it("Should instantiate Caliente Firebase project", () => {
    // expect(caliente).toBeInstanceOf(app.App)
  })
})
